# This program shows replacement of the formatter in formatting

formatter = "{} {} {} {}"

print(formatter.format(1, 2, 3, 4))
print(formatter.format("ek", "do", "teen", "Chaar"))
print(formatter.format(formatter, formatter, formatter, formatter))
